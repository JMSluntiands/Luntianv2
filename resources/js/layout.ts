/**
 * Layout entry – loaded on every dashboard layout page.
 * Enables Vite dev server live refresh when no other Vite entry is used (e.g. LBS Add).
 * Sidebar dropdowns are inited inline in dashboard layout so they always run.
 */
export {};
